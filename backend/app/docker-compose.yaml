services:
  db:
    image: postgis/postgis:16-3.4
    container_name: nus-nav-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: routesnus123
      POSTGRES_DB: nus_nav
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: